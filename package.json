{
  "name": "9-desafio-papel-tijera",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.5.9",
    "parcel": "^2.9.3",
    "type": "^2.7.2",
    "typescript": "^5.3.2"
  },
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /piedra-papel-tijera",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "gh-pages": "^6.0.0"
  }
}
